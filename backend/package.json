{
  "name": "mern-stack-task",
  "version": "1.0.0",
  "description": "MERN Stack application with About Us, Booking, and 404 pages",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "client": "cd ../client && npm start",
    "server": "nodemon server.js",
    "build": "cd ../client && npm run build",
    "seed": "node seedData.js",
    "heroku-postbuild": "npm install && cd ../client && npm install && npm run build"
  },
  "keywords": ["mern", "react", "express", "mongodb", "nodejs"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.5.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "body-parser": "^1.20.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
